#ifndef INPUT_VAL_H
#define INPUT_VAL_H

bool isValidFormat(const int numArgsRead, const int numArgsNeed);
int getValidInt(const char* prompt);  // Add this line if not already present
double getValidDouble(const char* prompt);

#endif // INPUT_VAL_H
